# Next parallel workstream plan (2026-07-17)

The first research-only job is now complete through
`RESEARCH_ASSESSMENT_READY`, but it is a permanent quality-BLOCK historical
fixture. Waiting for it to become Paper would be incorrect.

## Parallel tracks

1. **Candidate track — Codex, ready now:**
   `agent_tasks/codex__researchjob_prospective_candidate_diagnostic.md`.
   Read-only diagnosis of why the current inventory has no separate prospective
   quality-ALLOW candidate.
2. **Core-feature track — Gemini, ready now:**
   `agent_tasks/gemini__codex__paper_quant_sandbox_goal_architecture.md`.
   Read-only design for the structured-prompt runner, deterministic PaperPlan
   boundary and isolated paper/quant simulator using synthetic fixtures.

The second track can proceed without a real ALLOW symbol. Only the final
integration with a real job, OwnerDecision and PaperPlan remains gated. No new
Grok validation is needed now.
