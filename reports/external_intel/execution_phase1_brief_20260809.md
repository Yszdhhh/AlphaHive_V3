# 执行层 Phase 1：离线模拟执行增强——背景简报（2026-08-09）

> 用途：codex-sol 出实施规划。背景 + 约束 + 待规划问题。请结合仓库实际代码。

## 1. 现状

- **143_paper_trade.py**（每日 08:40）：读 `reports/forward_replay_returns.csv` 事件 → 四账户纸面结算。
  - A（统计口径）：事件后下一根 1h bar open 入场，固定 24h 时间退出，成本 27bps 单边（taker 22 + 滑点 5）
  - B（风控）：止损 -20% / trailing 50% / 上限 168h / MDD 断路器（2026-08-08 Owner 签批 -10%→-20%）
  - C（4h 确认）／D（s009 新币×确认，251 笔 +$1686，弱市 α +2.59% CI[+0.49,+4.47]）
  - 入场/退出价：binance_free_db raw_1h klines；幂等按 alert_id
- **botv2 执行层审查结论（codex，2026-08-09）**：整体 NO-GO，作需求库；Phase 1 = 离线模拟增强可做（滑点模型/部分成交/点差分层），测试网/live 独立网关 T3 PARK。

## 2. Phase 1 目标：离线模拟执行增强（不碰触发/不碰 A 统计口径锚）

目的：纸面模拟更接近真实可执行性，回答"事件在真交易里能不能以这个价格成交、成本到底多少"。
已知证据：E20 低流动性（168h +2.60%，滑点可忽略因名义 $1000 太小）；新币点差更大（E25 代理点差/冲击待补）；180 止损路径问题。

## 3. 约束（硬）

1. **A 账户 = 统计口径锚**：24h 持有 + 27bps 必须保持不变（与 109 统计口径的可比性），增强只能以"新增列/新报告/新账户"形式，绝不改 A 语义。
2. 不碰触发、不碰 B/C/D 现有逻辑（除非评估证明需要，且 Owner 签批）。
3. 纯离线（klines 模拟），不接交易所、不生成真实下单代码；测试网/live 不在本阶段。
4. 个人系统：单机 pandas，避免过度工程；新增依赖不引入。
5. 496 tests 全过；git diff --check 干净。

## 4. 请规划（文件级）

1. **执行模型设计**：滑点（按事件名义 $1000 与 24h 成交额占比 → 冲击模型；E20 已证 $1000/151M≈0.007% 可忽略——是否仍有必要？还是只对低流动性新币生效）、部分成交（事件量 vs 盘口深度不可得时的近似——用 1h 成交额占比分档）、点差分层（用 108 候选的 liquidity_24h_usd 或成交额分位代理）、资金费（持仓跨 8h 结算时点计费）。
2. **接入位置**：143 内加"增强执行"开关/列，还是新脚本 216 做"执行模拟复盘"（不改 143）？给出取舍（幂等、报告、对账）。
3. **与 A 锚的兼容**：新增什么列/报告段，向前兼容旧累计 CSV。
4. **验证设计**：怎么证明增强模型本身合理（如：A 口径 27bps vs 增强模型在 D 账户 251 笔上的成本差分布）？
5. **测试清单 + 验收**。
6. **明确不做**（Phase 1 边界）。
7. 风险与回滚。
输出 Markdown 到 stdout，务实可直接照做。
