# PROSPECTIVE-LIFECYCLE-PREFLIGHT-001

**Agent:** Gemini / antigravity  
**Tier:** T1/T2 read-only architecture and implementation preflight  
**Exact Desktop output:** `C:\Users\10639\Desktop\AlphaHive_V3_A_DATA_HEALTH_deliverables\agent_outputs\antigravity\PROSPECTIVE-LIFECYCLE-PREFLIGHT-001.md`

## Objective

Prepare the smallest implementation-ready plan for a future fresh,
registry-authorized prospective scan candidate to enter the immutable
ResearchJob chain and, only after all separate gates, a real PaperPlan
lifecycle. Identify precisely what is safe T1/T2 engineering and what remains
T3 Owner-only. This is design only.

## Required reading

Read, in order, the shared files named by
`G:\Quant test\AlphaHive_V3\PROJECT_REQUIRED_READING.md`, then this exact
task file, plus:

- `reports\LOCAL_RESEARCH_PAPER_OUTBOX_FOUNDATION_CODEX_HANDOFF_20260719.md`
- `reports\LOCAL_RESEARCH_PAPER_OUTBOX_FOUNDATION_FINAL_AUDIT_ACCEPTANCE_20260719.md`
- `harness\lib\candidate_research_job_bridge.py`
- `harness\lib\local_paper_plan_ledger.py`
- `harness\lib\local_notification_outbox.py`
- `harness\lib\paper_plan_engine.py`
- `alpha_hive\server\research_job_{service,repository,routes}.py`
- `config\research_orchestration_contract.yaml`
- `config\paper_execution_presets.yaml`

## Required output

1. Map the exact state/data boundaries from candidate run through ResearchJob,
   evidence, verification, assessment, per-job OwnerDecision, PaperPlan and
   dry-run notification. Identify which current local modules can be reused.
2. State a minimal Codex file allowlist, API/state-machine changes, immutable
   artifact and event/pointer requirements, crash/concurrency/tamper tests.
3. Explicitly separate T1/T2 construction from T3 gates: a real per-job Owner
   decision, Paper execution, trigger, Feishu credentials/recipients/delivery,
   and trade must remain PARK.
4. Reject historical, stale, registry-unapproved, quality-BLOCK and
   `performance_eligible=false` inputs. Do not propose relaxing a gate.
5. Include an acceptance matrix, self-check and exact follow-on dispatches.

## Hard boundaries

Read only. Write only the exact Desktop report. Do not modify a repository,
configuration, run/job/result artifact, scheduler, credential or browser. Do
not call a provider/network, invoke any route, create a job, submit evidence,
create a PaperPlan, send a notification, enable a trigger or trade.

Use `GREEN`, `PARK`, or `FAIL`; if an authoritative input is absent, use PARK.
