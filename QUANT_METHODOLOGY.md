# AlphaHive 量化系统方法论 v1（策略工厂宪法）

> 目的：把"科学测试 → 可复现 edge → 可盈利执行"的完整方法论固化为本项目操作依据。
> 所有策略开发必须按本文档管线执行；违反统计纪律的结果一律标 `exploratory` 不得升级。
> 本文档是活文档：方法论演进时更新版本号并在决策记录留痕。

## 1. Edge 来源分类学（edge 只能来自这四类，先归因再开发）

| 类别 | 机制 | 寿命 | 项目实例 |
|---|---|---|---|
| E-A 机制/结构摩擦 | 清算级联、资金费率、杠杆拥挤、流动性断层——市场机制本身制造的错价 | 中-长（结构不消失） | wash_cvd（清杠杆后卖压枯竭）、liq 激增筛选 |
| E-B 情绪/行为偏差 | 超卖恐慌、FOMO、处置效应、锚定——人类行为的统计规律 | 中（人群结构变化时衰减） | 贪婪层 fng≥60、恐惧抄底 |
| E-C 信息/价格发现延迟 | 大单、巨鲸、事件驱动、跨市场领先——信息传播需要时间 | 短-中（套利者涌入即亡） | ETF 净流入（已证无）、CME 机构 OI |
| E-D 结构性供需 | 稳定币发行、解锁、ETF 流动、季末调仓——资金流有周期性 | 长但难抓（数据滞后） | 交易所净流入（描述性，待复核） |

**规则**：新策略必须声明 edge 类别 + 为什么这个类别的错价会持续存在（寿命论证）。
写不出机制的因子直接淘汰——统计显著但无机制的因子是过拟合第一名。

## 2. 因子/策略开发管线（10 步，硬性顺序）

1. **Alpha Card 预注册**（`strategies/<id>/alpha_card.md`，10 字段）：
   asset/freq/universe/holding/rebalance/mechanism/data/constraints/similar/failure
   —— 写清楚"什么算证伪"，禁止事后改。
2. **对抗审查**（自审 + 可选 grok/gpt 第二意见）：机制/替代解释/泄漏/障碍/公式/可证伪性。
3. **锁定可执行定义**：公式、输入、频率、滞后、缺失处理、winsorize/标准化、成本、再平衡。
4. **数据可得性审计**：数据源、粒度、历史深度、免费额度、更新频率。
5. **单策略健康检查**：n、均值、超额、bootstrap CI、胜率、MFE/MAE、分 episode 稳定性。
6. **稳健性**：阈值敏感性（±20%）、子样本（episode 切分）、seed 敏感性、匹配/分层基线。
7. **增量检验**：相对已有策略的 marginal 贡献（组合后是否还有正边际）。
8. **成本后净值**：taker/maker + 滑点 + funding/融资成本，净期望必须 > 0 且 > 1.5× 成本。
9. **前向影子**：事件计数窗口（30 预警 / 60-100 正式），与虚拟交易双账户并行。
10. **晋级决策**：仅前向独立通过 + Owner 签批 → shadow 转 live。

**每条线消耗检验预算**：季度 1 主问题 + 2 预注册次假设（`QUANT_PRE_REGISTRY.md` 记账）。

## 3. 统计纪律

- 新因子 t-stat 门槛 **≥3.0**（Harvey-Liu-Zhu，M≈20-100）；探索性结果强制标 `exploratory`。
- 基线必须同期可比（同区间随机横截面/时间点），禁止全样本混合。
- 事件/截面相关未 cluster 时 CI 偏窄——报告标注，不假装精确。
- 非显著 = 功效不足或无效，**不许宣称"已证伪"**（除非做了 MDE/TOST）。
- 独立样本复核：同一发现换数据窗口/换市场仍成立才算升级；同窗子集不算独立确认。
- 预注册文件 + 消耗日志是升级的前提；未登记结果不得进 shadow。

## 4. 执行细则（可盈利的最后一道闸）

### 4.1 成本模型（先算成本再谈 alpha）
- 单边：taker 22bps（加密永续）+ 滑点 5bps = **27bps**；round-trip ≈ 54bps。
- 美股永续/合成（Delta/gTrade/Ostium）：taker 费率不同，按平台文档另测（默认 10-20bps）。
- 资金费率：持有多空仓跨资金结算时点，计入成本分解（alpha_hive exec_planner 的 TOTAL_COST_BPS 口径）。
- 每策略必须做**成本敏感性**：0.5×/1×/2× 成本下净期望是否仍正。

### 4.2 订单执行
- 入场：信号后下一 bar open（禁止用信号时点收盘价——虚拟交易已按此口径）。
- 大单拆分（>5% 日均量）：按 volume 加权切片，单笔 ≤ 当日量 1%。
- 出场上限 168h（时间退出）或止损/trailing（风控账户 B 规则）。

### 4.3 仓位与风控（参数锁定自 alpha_hive chassis_engine，经验证不重新发明）
- 单策略仓位：vol-target 20% 年化；事件名义 ≤ $1,000/笔（当前积累期）。
- 组合级：单资产集中度 ≤ 40%；净值回撤 -15% 减半 / -25% 停新仓（MDD 断路器）。
- 单笔止损 -10%（仅风控账户）；trailing 峰值回落 50%。
- 相关性闸：新策略与现有策略收益相关 > 0.7 → 合并管理，不单独开仓。
- 保证金/杠杆：永续杠杆 ≤ 3×；合成资产按平台维持保证金率预留。

### 4.4 全天候运行
- 7×24 扫描（108 类）→ 前向影子（109 类）→ 虚拟交易（143 类）→ 飞书通知。
- 每策略独立事件流 + 独立净值；decay 监测按策略配置（事件计数窗口 + CUSUM）。
- regime 标签（VIX/广度/贪婪/宏观）贯穿每笔事件，衰退时先限缩适用域再退役。

## 5. 策略组合逻辑

- **组合的是收益流，不是信号**：多策略只要收益相关度低就有分散价值。
- 目标组合 = 4-8 条策略线，覆盖不同 edge 类别和资产类别：
  - 机制类（wash_cvd 山寨 / 美股高波动 washout）——低频高确定性
  - 情绪类（贪婪/恐惧分层）——中频
  - 结构类（funding 拥挤度、解锁/ETF 流）——低频大样本
  - 跨市场对冲（BTC 崩→避险资产）——尾部保护
- 每条线独立验收，通过才进组合；组合权重先等权，marginal 检验后调整。

## 6. 失败哲学（比成功更重要的规则）

- 因子失败不是项目失败：**证伪是产出**（旧项目 20+ HARD_FAIL 的教训全部写进本宪法）。
- 每个退役策略写 GRAVEYARD 条目：为什么失败、哪个环节错了、如何避免重犯。
- 换手率是成本之父：年换手 > 365 的横截面因子直接判死（旧项目教训）。
- 前向验证是唯一真相：历史显著只是候选资格，不是部署许可。

*版本历史：v1 2026-08-07 建立（Owner 授权开发全市场多策略体系；整合 115/131/133/134/141/142/143/144 全部经验 + alpha_hive gauntlet 教训 + Harvey-Liu-Zhu 纪律）。*
