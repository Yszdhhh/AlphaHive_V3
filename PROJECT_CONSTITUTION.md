# AlphaHive V3 项目宪法

## 系统本质与成功标准

AlphaHive V3 是一个可审计的异常研究与纸面执行纪律系统。它不是自动交易 bot，也不是因子机器。系统的职责是把冻结的异常候选变成可复核的研究样本、证据与人工检查项；它不替 Owner 做交易决策。

项目 KPI 是每周可复盘样本数，以及被证伪或被验证的假设数，不是 Sharpe、收益或任何单一绩效数字。

## 打分与执行纪律

异常打分只是审查排序参考，不是综合 alpha 分数，不驱动杠杆或仓位。执行参数来自风控纪律：vol-target、结构性失效条件与成本底；执行参数不来自异常打分。

配置版本锁以 `config/` 下各配置文件自己的版本字段和版本锁注释为单一真理源；本宪法只引用该来源，不复制具体版本值。

## 护城河假设

项目的护城河假设是 Capacity Edge。必须诚实标注：第 1 周不验证 Capacity Edge。验证所需的点差与深度数据正是当前最缺的证据；点差过滤第 1 周关闭，衍生数据仅约 23.6 个月（自 2024-06 起）。

已证伪方向先验统一引用 [`GRAVEYARD.md`](GRAVEYARD.md)。墓地不是可绕过的建议清单，而是研究阶段必须读取的负面先验；任何复用都需要新的、可审计的证据与 Owner 决策。

## 数据边界

- Klines 约 53 个月（约自 2021-12）。
- Funding、OI 等衍生数据约 23.6 个月（约自 2024-06）。
- 免费数据源不提供钱包标签；不得把地址身份、聪明钱或资金主体身份当作已知事实。
- 点差、深度与身份检查缺失时必须显式标注缺口，不得用估计值冒充真实检查结果。

## 决策权与安全红线

Owner 是唯一决策者。阈值只能向更严格方向调整；任何 agent 都不能签署决策、批准纸面执行或下单。研究产物不得生成真实下单代码，不得触碰执行层，不得把研究排序结果转换为杠杆或仓位。

## 四条强约束

1. Karpathy：先想后写，能简则简，做外科手术式改动；目标必须可验证，遇到困惑要公开说明，不隐藏问题。
2. 预注册：本轮范围锁定 T0–T5，不扩产，不优化范围外代码。
3. 回归闸：改动后既有单测与 prompt 包构建必须通过；非预期行为变化就停下报告，不自行修复。
4. 单一真理源：宪法一份、墓地一份，其余位置只引用，不复制治理正文。

## 协同原则

角色无关地保持运动员/裁判分离：执行者提交可审计改动，架构席独立验收。研究区保持 provider neutrality，可接入多个可替换 provider；未验证产物必须隔离并标记 `UNVERIFIED`。建设区统一自动加载本宪法与墓地，研究区依靠研究提示词显式携带墓地约束。
